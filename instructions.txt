You are a senior DevSecOps engineer designing a production-grade secrets lifecycle automation system for a payment orchestration platform (similar to Yuno).

Your task is to generate a complete, working solution that satisfies ALL requirements defined in the provided context.md.

## Instructions

- Follow the context strictly. Do NOT invent requirements outside of it.
- Optimize for security, auditability, and reliability over simplicity.
- Prefer clear, minimal, production-ready implementations over complex abstractions.
- All components must work together coherently as a system.
- Avoid unnecessary verbosity in code and explanations.

---

## Output Requirements

Generate a full repository structure including:

### 1. Rotation System
- Implement an automated rotation workflow (script or pipeline)
- Must include:
  - versioned secrets
  - propagation simulation
  - health checks
  - rollback logic
  - grace period handling
- Must demonstrate zero-downtime strategy (dual secret support)

---

### 2. Secrets & Access Control
- Define a secrets structure
- Provide IAM/RBAC-style policies (cloud-agnostic but AWS-compatible)
- Enforce strict least privilege per service

---

### 3. Audit Logging
- Implement logging mechanism capturing:
  - timestamp
  - identity
  - secret_id
  - action
  - status
- Logs must be append-only and separated from secrets storage

---

### 4. Developer CLI
- Provide a CLI or script to fetch secrets securely
- Must:
  - require authentication (mock acceptable)
  - return short-lived secrets
  - log all access

---

### 5. Break Glass Runbook
- Provide a markdown document describing:
  - emergency access procedure
  - safeguards
  - audit requirements

---

### 6. Threat Model & Design Doc
Create a document including:

- Threat model (attack scenarios)
- Design decisions and trade-offs:
  - security vs developer experience
  - speed vs safety
- Residual risks
- PCI-DSS mapping (Req 3, 8, 10)

---

### 7. README
Include:
- Architecture explanation
- Setup instructions
- How to run rotation
- Example outputs

---

## Technical Constraints

- Keep solution cloud-agnostic but align with AWS patterns
- Mock external provider APIs
- Do NOT use hardcoded secrets
- Use clear structure and naming
- Keep implementation concise but realistic

---

## Quality Expectations

- Idempotent operations
- Safe failure handling (no broken states)
- Clear logs and outputs
- Security best practices applied consistently

---

## Important

Do NOT:
- Skip audit logging
- Use overly permissive access
- Assume manual steps
- Ignore rollback scenarios

---

Generate all files with clean structure and production-quality clarity.
